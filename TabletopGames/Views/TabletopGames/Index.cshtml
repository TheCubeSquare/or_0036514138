@model TabletopGames.ViewModels.TabletopGamesViewModel

@{
    ViewBag.Title = "Tabletop Games";
}

<h2>Tabletop Games List</h2>

<a asp-action="Create"
   asp-route-page="@Model.PagingInfo.CurrentPage"
   asp-route-sort="@Model.PagingInfo.Sort"
   asp-route-ascending="@Model.PagingInfo.Ascending">Add Tabletop Game</a>

<pager page-info="@Model.PagingInfo" page-action="Index" page-title="Input desired page"
       class="float-end">
</pager>

<table class="table table-sm table-striped table-hover">
    <thead>
        <tr>
            <th>
                <a asp-route-sort="1" asp-route-page="@Model.PagingInfo.CurrentPage" asp-route-ascending="@(Model.PagingInfo.Sort == 1 ? !Model.PagingInfo.Ascending : true)">
                    ID
                </a>
            </th>

            <th>
                <a asp-route-sort="2" asp-route-page="@Model.PagingInfo.CurrentPage" asp-route-ascending="@(Model.PagingInfo.Sort == 2 ? !Model.PagingInfo.Ascending : true)">
                    Name
                </a>
            </th>
            <th>
                <a asp-route-sort="3" asp-route-page="@Model.PagingInfo.CurrentPage" asp-route-ascending="@(Model.PagingInfo.Sort == 3 ? !Model.PagingInfo.Ascending : true)">
                    Year
                </a>
            </th>
            <th>
                <a asp-route-sort="4" asp-route-page="@Model.PagingInfo.CurrentPage" asp-route-ascending="@(Model.PagingInfo.Sort == 3 ? !Model.PagingInfo.Ascending : true)">
                    Min Players
                </a>
            </th>
            <th>
                <a asp-route-sort="5" asp-route-page="@Model.PagingInfo.CurrentPage" asp-route-ascending="@(Model.PagingInfo.Sort == 3 ? !Model.PagingInfo.Ascending : true)">
                    Max Players
                </a>
            </th>
            <th>
                <a asp-route-sort="6" asp-route-page="@Model.PagingInfo.CurrentPage" asp-route-ascending="@(Model.PagingInfo.Sort == 3 ? !Model.PagingInfo.Ascending : true)">
                    Average Rating
                </a>
            </th>
            <th>
                <a asp-route-sort="7" asp-route-page="@Model.PagingInfo.CurrentPage" asp-route-ascending="@(Model.PagingInfo.Sort == 3 ? !Model.PagingInfo.Ascending : true)">
                    Average Complexity
                </a>
            </th>
            <th>
                <a asp-route-sort="8" asp-route-page="@Model.PagingInfo.CurrentPage" asp-route-ascending="@(Model.PagingInfo.Sort == 3 ? !Model.PagingInfo.Ascending : true)">
                    PlayTime
                </a>
            </th>

            <th>
            </th>
            <th>
            </th>
        </tr>

    </thead>
    <tbody>
        @foreach (var izv in Model.TabletopGames)
        {
        <tr>
            <td class="text-left">@izv.IdGame</td>
            <td class="text-left">@izv.NameGame</td>
            <td class="text-left">@izv.YearGame</td>
            <td class="text-left">@izv.MinPlayers</td>
            <td class="text-left">@izv.MaxPlayers</td>
            <td class="text-left">@izv.AverageRating</td>
            <td class="text-left">@izv.AverageComplexity</td>
            <td class="text-left">@izv.PlayTime</td>

            <td>
                <a asp-action="Edit"
                   asp-route-id="@izv.IdGame"
                   asp-route-page="@Model.PagingInfo.CurrentPage"
                   asp-route-sort="@Model.PagingInfo.Sort"
                   asp-route-ascending="@Model.PagingInfo.Ascending"
                   class="btn btn-sm btn-success"
                   title="Edit">Edit</a>
            </td>

            <td>
                <form asp-action="Delete" method="post"
                      asp-route-id="@izv.IdGame"
                      asp-route-page="@Model.PagingInfo.CurrentPage"
                      asp-route-sort="@Model.PagingInfo.Sort"
                      asp-route-ascending="@Model.PagingInfo.Ascending"
                      onclick="return confirm('Are you sure?')">
                    <button class="btn btn-sm btn-danger delete"
                            title="Delete">
                        Delete
                    </button>
                </form>
            </td>

        </tr>
        }
    </tbody>
</table>

@section scripts {
<script src="~/js/pager.js" asp-append-version="true"></script>
}